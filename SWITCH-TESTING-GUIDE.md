# คู่มือทดสอบดึงข้อมูลอุปกรณ์จริง — Switch Alcatel OS6450-24

คู่มือนี้ใช้ทดสอบว่าระบบ UP NMS (รันแบบ Local บนเครื่อง) ดึงข้อมูลจาก Switch จริงได้หรือไม่
ครอบคลุมตั้งแต่เตรียมเครือข่าย ตั้งค่าสวิตช์ ทดสอบ SNMP จนถึงเพิ่มเข้าระบบและยืนยันผล

---

## ภาพรวม — ทำ 6 เฟสตามลำดับ

| เฟส | ทำอะไร | รันที่ไหน |
|---|---|---|
| 1 | เตรียมเครือข่าย ต่อสาย หา IP | สวิตช์ (Console) + Mac |
| 2 | เช็ค/ตั้งค่า SNMP บนสวิตช์ | สวิตช์ (Console) |
| 3 | ทดสอบ SNMP จาก Mac | Mac (Terminal) |
| 4 | เพิ่มอุปกรณ์เข้าระบบ | ไฟล์ `.env` + เว็บ |
| 5 | ยืนยันผลลัพธ์ | Docker log + เว็บ |
| 6 | แก้ปัญหาที่พบบ่อย | อ้างอิงตอนติดปัญหา |

---

## เฟส 1 — เตรียมเครือข่ายให้ Mac คุยกับสวิตช์ได้

### 1.1 ต่อสาย Console เข้าสวิตช์ (ครั้งแรกเท่านั้น เพื่อตั้งค่าเบื้องต้น)

เสียบสาย Console (RJ45-to-USB) จาก Mac ไปพอร์ต Console ของสวิตช์ เปิด Terminal บน Mac:

```bash
ls /dev/tty.*                        # หาชื่อพอร์ต USB serial ก่อน
screen /dev/tty.usbserial-XXXX 9600  # แทน XXXX ด้วยชื่อที่เจอจริง
```

ออกจาก session: กด `Ctrl+A` ตามด้วย `K` แล้วกด `Y`

### 1.2 เช็คว่าพอร์ตไหนมีสายเชื่อมต่อจริง (ที่ CLI สวิตช์)

```
-> show interfaces status
```

ดูคอลัมน์ **DETECTED** — พอร์ตที่มีสายเสียบจริงจะขึ้นค่า (เช่น `100 Full`) ไม่ใช่ `-`

> **ถ้าทุกพอร์ตขึ้น `-` หมด**: ยังไม่มีสาย LAN เสียบเข้าสวิตช์เลย ต้องเอาสายมาเสียบเข้าพอร์ตใดก็ได้
> (เช่น `1/1`) แล้วรันคำสั่งนี้ซ้ำจนเห็นค่าจริง

### 1.3 เช็ค Mac ก่อน — มีกี่ Network Interface (สำคัญมาก ป้องกันปัญหาที่พบบ่อยที่สุด)

Mac ที่มี USB-C Hub/Dock อาจมีหลาย interface พร้อมกัน (`en0`, `en1`, `en4`, ...) ทำให้ macOS
**เลือกเส้นทางผิด** ไปหาสวิตช์ได้ ให้เช็คก่อนเสมอ:

```bash
ifconfig | grep "inet "
```

จดบันทึก IP ที่เจอทั้งหมดไว้ก่อน แล้วเช็คว่า interface ไหนคือ USB-Ethernet adapter ที่ต่อสายไปสวิตช์จริง:

```bash
ifconfig en1    # เปลี่ยนเลขตามที่มีในเครื่อง ลองไล่ทีละตัว
```

**ตัวที่ใช้งานได้จริงต้องขึ้น `status: active`** (ถ้าขึ้น `inactive` แปลว่าตัวนั้นไม่ได้ต่อสายอยู่จริง
แม้จะเคยมี IP ค้างอยู่ก็ตาม)

### 1.4 ตั้ง IP แบบ Static ให้สวิตช์ (แนะนำ แทน DHCP เพื่อตัดปัญหา)

หา subnet ของ Mac ก่อน (จาก interface ที่ active ในขั้น 1.3) แล้วไปตั้งที่ CLI สวิตช์:

```
-> ip interface ?                                              # เช็ค syntax รุ่นนี้ก่อนเสมอ
-> ip interface "vlan1-mgmt" address 10.50.18.50 mask 255.255.255.0 vlan 1
-> show ip interface                                            # ยืนยัน Status = UP
```

**เลือกเลข IP ท้ายให้ต่างจากทุก IP ที่ Mac มีอยู่แล้ว** (เช็คจากขั้น 1.3 ก่อนเสมอ กันชนกัน)

### 1.5 ทดสอบ ping จาก Mac

```bash
ping 10.50.18.50
```

ต้องเห็น `64 bytes from 10.50.18.50...` — **ถ้า timeout หรือขึ้น "No route to host"/"Host is down"**
ให้ข้ามไปอ่านหัวข้อ **"ปัญหาที่ 1"** ในเฟส 6 ก่อน อย่าเพิ่งไปเฟสถัดไป

---

## เฟส 2 — เช็ค/ตั้งค่า SNMP บนสวิตช์

### 2.1 เช็คว่าเปิดอยู่แล้วหรือยัง (ทำก่อนเสมอ อาจไม่ต้องตั้งอะไรเลย)

```
-> show snmp community-map
```

ถ้าเห็นผลลัพธ์แบบนี้ **แปลว่าเปิดพร้อมใช้แล้ว ข้ามไปเฟส 3 ได้เลย**:
```
status    community string    user name
enabled   public              public
```

### 2.2 ถ้ายังไม่มี/ไม่ enabled — เช็ค syntax ก่อนตั้งค่า

```
-> snmp ?                    # ดู sub-command ที่รุ่นนี้รองรับจริง
-> snmp community ?          # ไล่ดูรูปแบบคำสั่งที่ถูกต้อง
```

รูปแบบที่เป็นไปได้ (เลือกตามที่เห็นจริงจากคำสั่ง `?` ด้านบน — **อย่าเดาโดยไม่เช็คก่อน**):
```
-> snmp community map public user public enable
```

---

## เฟส 3 — ทดสอบ SNMP จาก Mac (ก่อนเอาเข้าระบบเสมอ)

### 3.1 ติดตั้งเครื่องมือ (ครั้งแรกเท่านั้น)

```bash
brew install net-snmp
```

### 3.2 ทดสอบทีละคำสั่ง (ใช้ IP สวิตช์จริงที่ตั้งไว้ในเฟส 1.4)

```bash
# คำสั่งที่ 1 — เช็คว่าตอบ SNMP พื้นฐานไหม
snmpwalk -v2c -c public 10.50.18.50 sysDescr.0

# คำสั่งที่ 2 — ทดสอบดึงค่า CPU
snmpwalk -v2c -c public 10.50.18.50 1.3.6.1.2.1.25.3.3.1.2

# คำสั่งที่ 3 — ทดสอบดึงสถานะพอร์ตทั้งหมด (ควรเห็น 24 บรรทัด)
snmpwalk -v2c -c public 10.50.18.50 1.3.6.1.2.1.2.2.1.8
```

**ผ่านทั้ง 3 คำสั่ง (ไม่ timeout) = พร้อมเอาเข้าระบบจริงได้แล้ว ไปเฟส 4 ต่อ**

---

## เฟส 4 — เพิ่มสวิตช์เข้าระบบ UP NMS

### 4.1 เปิด `.env` ที่ root โปรเจกต์ (ตำแหน่งเดียวกับ `docker-compose.yml`)

```
SNMP_PROVIDER=real
```
*(เดิมเป็น `mock` — ต้องเปลี่ยนเป็น `real` ระบบถึงจะยิง SNMP จริง)*

### 4.2 Rebuild backend

```bash
docker compose up -d --build backend
```

### 4.3 เพิ่มอุปกรณ์ผ่านเว็บ

เข้า `http://localhost:3000` แล้ว login เพิ่มโซน (ถ้ายังไม่มี) แล้วเพิ่มอุปกรณ์:
- ประเภท: Switch
- ชื่อ: ตั้งเอง เช่น `SW-TEST-01`
- IP Address: `10.50.18.50` (หรือ IP จริงที่ทดสอบผ่านในเฟส 3)
- Brand: `Alcatel-Lucent` / Model: `OS6450-24`

---

## เฟส 5 — ยืนยันว่าดึงข้อมูลจริงได้

### 5.1 ดู log backend ระหว่างรอ (เปิดหน้าต่างนี้ค้างไว้)

```bash
docker compose logs -f backend
```

### 5.2 รอ 1 รอบ poll (ทุก 5 นาทีตามค่าเริ่มต้น)

### 5.3 เข้าไปดูหน้ารายละเอียดอุปกรณ์ (คลิก `SW-TEST-01`)

**สำเร็จ** = สถานะเป็น Online, ค่า CPU/Memory ไม่ใช่ 0, ตารางพอร์ตตรงกับพอร์ตจริงที่มีสายเสียบ

**ถ้ายังเห็นค่า 0 หลังรอครบ 5 นาที** ให้เช็ค log (5.1) แล้วไปอ่านหัวข้อ "ปัญหาที่ 2" ด้านล่าง

---

## เฟส 6 — ปัญหาที่พบบ่อย (อ้างอิงตอนติดปัญหา)

### ปัญหาที่ 1 — ping ไม่ผ่าน / "No route to host" / "Host is down"

**สาเหตุที่พบบ่อยที่สุด**: Mac มีหลาย network interface พร้อมกัน (USB-C Hub/Dock) ทำให้ macOS
เลือกเส้นทางผิดไปหาสวิตช์ (ไปทาง Wi-Fi/เครือข่ายเดิมแทนที่จะไปทางสาย USB-Ethernet ที่ต่อสวิตช์)

**วิธีวินิจฉัย**:
```bash
route get 10.50.18.50
```
ดูบรรทัด `interface:` ถ้าไม่ใช่ตัวที่ต่อสายไปสวิตช์จริง (เช็คจากเฟส 1.3) นั่นคือสาเหตุ

**วิธีแก้**:
1. เช็คว่าสาย USB-C-to-Ethernet adapter เสียบแน่น ลองถอด-เสียบใหม่
2. เช็ค `ifconfig en1` (หรือเลขที่ตรงกับ adapter) ว่าเปลี่ยนเป็น `status: active` แล้วหรือยัง
3. ถ้ายังเลือกผิดทาง ลองปิด Wi-Fi ชั่วคราว หรือถอด adapter อื่นที่ไม่เกี่ยวออกก่อน บังคับให้เหลือ
   เส้นทางเดียวไปสวิตช์

### ปัญหาที่ 2 — SNMP timeout ทั้งที่ ping ผ่านแล้ว

**เช็คทีละจุด**:
```
-> show snmp station        # เช็คว่ามี ACL จำกัด IP ที่ยิงมาได้ไหม
-> show snmp statistics     # เช็คว่า request ไปถึงสวิตช์จริงไหม (รันก่อน-หลัง snmpwalk เทียบตัวเลข)
```

### ปัญหาที่ 3 — ทดสอบจาก Mac ผ่าน แต่ระบบ UP NMS ยังดึงไม่ได้ (เฟส 5)

Docker container อาจเข้าถึงเครือข่ายสวิตช์ไม่ได้ทั้งที่ Mac เข้าถึงได้ (เพราะ Docker มีเครือข่ายแยก
ของตัวเอง) ทดสอบจากข้างใน container โดยตรง:

```bash
docker compose exec backend sh -c "apk add net-snmp-tools && snmpwalk -v2c -c public 10.50.18.50 sysDescr.0"
```

ถ้าจากข้างนอก (Mac) ผ่านแต่ข้างใน container ไม่ผ่าน เป็นปัญหาการตั้งค่าเครือข่ายของ Docker
บน macOS (Docker Desktop/OrbStack) ที่ต้องตั้งค่าเพิ่มเติมแยกต่างหาก

### ปัญหาที่ 4 — คำสั่งบนสวิตช์ error "Invalid entry"

AOS (ระบบปฏิบัติการของ Alcatel) มีหลายเวอร์ชันที่ syntax ต่างกัน ห้ามเดาคำสั่งจากคู่มือทั่วไป
โดยไม่เช็คก่อน ให้พิมพ์ `<คำสั่งที่จะใช้> ?` เสมอก่อนรันจริง เช่น `snmp ?`, `ip interface ?`
เพื่อดูรูปแบบที่รุ่น/เวอร์ชันเครื่องนั้นรองรับจริง
